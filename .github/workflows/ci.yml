name: CI Workflow

on:
  push:
    branches:
      - '**' #allows the workflow to run on each push to any branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: running code
        uses: python nonstandardcode.py



